
var margin = {top: 20, right: 20, bottom: 30, left: 30},
    width = 660 - margin.left - margin.right,
    height = 350 - margin.top - margin.bottom;

var parseDate = d3.time.format("%Y%m%d").parse;

var x = d3.scale.linear()
    .range([0, width]);

var y = d3.scale.linear()
    .range([height, 0]);

var xAxis = d3.svg.axis()
    .scale(x)
    .orient("bottom");

var yAxis = d3.svg.axis()
    .scale(y)
    .orient("left");

//Drawing curves

var set = [];

  for (i=0; i < 100; i++) {
    for (j=0; j < 2; j++) {
      set.push({i, j});
    }
  } 

console.log(JSON.stringify(set));

var line = d3.svg.line()
    .x(function(d) { drawGraph;})
    .y(function(d) { drawGraph; })
    .interpolate("cardinal");

var svg = d3.select("#vis").append("svg")
    .attr("width", width + margin.left + margin.right)
    .attr("height", height + margin.top + margin.bottom)
  .append("g")
    .attr("transform", "translate(" + margin.left + "," + margin.top + ")");

// the original data was loaded via:
// d3.tsv("data.tsv", function(error, data) {
//   data.forEach(function(d) {
//     d.date = parseDate(d.date);
//     d.low = +d.low;
//     d.high = +d.high;
//   });
//
// which won't work. instead, we have to parse the data_embed 
// we added to the page above like this:
var data = d3.csv.parse(data_embed['homeruns.csv'], function(d, i) {
  return {
    index: +i,
    hitter: d.Hitter,
    distance: +d.TrueDist,
    speed: +d.SpeedOffBat,
    elevation_angle: +d.Elev_Angle,
    apex: +d.Apex
  };
});

//console.log(JSON.stringify(data));

x.domain([0, d3.max(data, function(d) { return d.distance + 10; })]);
y.domain([0, d3.max(data, function(d) { return d.apex + 10; })]);

/*
  svg.append("path")
    .datum(data)
    .attr("class", "area")
    .attr("d", line);
*/

svg.append("g")
    .attr("class", "x axis")
    .attr("transform", "translate(0," + height + ")")
    .call(xAxis);

svg.append("g")
    .attr("class", "y axis")
    .call(yAxis)
  .append("text")
    .attr("transform", "rotate(-90)")
    .attr("y", 6)
    .attr("dy", ".71em")
    .style("text-anchor", "end")
    .text("height (ft)");

// optional image insert inside svg example:
svg.selectAll("image").data([0])
  .enter()
  .append("svg:image")
  .attr("x", width - 55)
  .attr("y", 5)
  .attr("width", "50")
  .attr("height", "50");

